<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:background="@color/bg_secondary"
	android:fitsSystemWindows="true"
	tools:context=".ui.activity.DeviceInfoActivity">
	
	<include layout="@layout/app_bar_device_select"
		android:id="@+id/app_bar"/>

	<uk.co.imallan.jellyrefresh.JellyRefreshLayout
		android:id="@+id/jellyRefreshLayout"
		android:text="@string/loading"
		android:textColor="@color/text_primary"
		app:jellyColor="@color/divider"
		app:layout_constraintTop_toBottomOf="@+id/app_bar"
		app:layout_constraintBottom_toBottomOf="parent"
		android:layout_width="match_parent"
		android:layout_height="0dp">
		
		<ScrollView
			android:layout_width="match_parent"
			android:layout_height="match_parent">
		
			<LinearLayout
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:orientation="vertical">
				
				<androidx.recyclerview.widget.RecyclerView
					android:id="@+id/recyclerView"
					android:layout_width="match_parent"
					android:layout_height="wrap_content"
					android:layout_marginTop="@dimen/dimen_7dp"
					android:layout_marginBottom="@dimen/dimen_7dp"
					android:visibility="visible"/>
				
				<TextView
					android:paddingTop="170dp"
					android:id="@+id/tv_empty"
					android:layout_width="wrap_content"
					android:layout_height="wrap_content"
					android:layout_gravity="center"
					android:drawablePadding="@dimen/dimen_10dp"
                    android:gravity="center"
                    android:text="@string/pull_to_refresh_list"
                    android:textColor="?attr/app_normal_color"
					android:layout_marginStart="30dp"
					android:layout_marginEnd="@dimen/dimen_30dp"
                    android:textSize="16sp"
                    android:visibility="visible"
                    app:drawableTint="?attr/app_normal_color"
                    app:drawableTintMode="src_atop"
                    app:drawableTopCompat="@mipmap/ic_empty_printer_list" />
			
			</LinearLayout>
		</ScrollView>
	</uk.co.imallan.jellyrefresh.JellyRefreshLayout>

	
</androidx.constraintlayout.widget.ConstraintLayout>